language: node_js
node_js:
  - "node"

cache: npm

before_script:
  - cd project
script:
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: project/dist
  on:
    branch: main